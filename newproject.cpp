#include<iostream>
#include<cmath>
using namespace std;
int main()
{
    long long n;
    cin>>n;
    cout<<"The next number for the number "<<abs(n)<<" is "<<abs(n)+1<<"."<<
endl;
cout<<"The previous number for the number "<<abs(n)<<" is "<<abs(n)-1<<".";
    return 0;
 
}
